---
import { SITE } from '../config/site';
import { BASE as base } from '../utils/base';
---

<header class="sticky top-3 z-20 mx-auto max-w-6xl px-4 sm:px-6">
  <div class="glass flex items-center justify-between px-4 py-3 sm:px-6">
    <a href={base} class="flex items-center gap-2.5 text-text">
      <span class="flex h-8 w-8 items-center justify-center rounded-full bg-accent/15 text-accent" aria-hidden="true">
        <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="1.8">
          <path d="M2 13c2-3 4-3 6 0s4 3 6 0 4-3 6 0" stroke-linecap="round" stroke-linejoin="round"></path>
        </svg>
      </span>
      <span class="text-base font-medium tracking-wide">{SITE.name}</span>
    </a>
    <nav aria-label="Primary" class="flex items-center gap-6 text-sm font-light text-text-muted">
      <a href={`${base}#mixer`} class="transition-colors duration-300 hover:text-text">Mixer</a>
      <a href={`${base}#faq`} class="hidden transition-colors duration-300 hover:text-text sm:inline">FAQ</a>
      <a href={`${base}about`} class="hidden transition-colors duration-300 hover:text-text sm:inline">About</a>
    </nav>
  </div>
</header>
